# Plan — Math Intuitions primer  *(SUPERSEDED)*

> **This plan has been completely replaced by `docs/syllabus-math-for-deep-learning-course.md`** — the Stanford/MIT-grade syllabus for the standalone course **"All the Math You Need for Deep Learning."**
>
> The new syllabus keeps the same site infrastructure that this plan established (folder `research-areas/math-intuitions/`, the `math-` slug prefix, group `Math Intuitions`, and the three already-written probability notes) but:
>
> - re-organizes everything around **ten question-driven lectures** instead of isolated object-blocks;
> - adds the **linear algebra / spectral / regression / optimization / information-theory** spine this plan lacked (vectors & similarity, SVD/PCA, spectral methods, least squares, gradients & matrix calculus, maximum likelihood, classification, entropy/cross-entropy/KL, ELBO, stochastic gradients, dynamics);
> - adopts a **three-tier** note design — gentle intuition → a **lightweight by-hand derivation** in the main line → optional `深一層` rigor boxes at Stanford/MIT elective level;
> - makes **read-derive-translate-explain** (read an equation, do its short derivation, translate it both ways, explain it) the explicit spine of every note;
> - folds the three existing notes (`math-randomness-and-distribution`, `math-density-vs-probability`, `math-expectation-and-averages`) into **Lecture 6**.
>
> The Diffusion & Flow course's "Part 0" pointer now resolves to the new syllabus. Do not author against this file.
