---
slug: "dma-w4-5-lab"
label: "dma-lab-flow-matching"
prereqs:
  - math-euler-error
lang: "zh"
title: "實作：同一個 toy，只換訓練目標"
category: "courses"
group: "Unit 2 · Flow Matching"
status: "available"
updated: 2026-09-13
summary: "沿用 diffusion 單元的 toy，只換掉訓練目標與取樣器：實作線性 FM 與 Euler／Heun 取樣，畫出步數對誤差的 log-log 圖，並親手驗證 ε 模型不重訓就能當速度場用。"
demos: []
references:
  - label: "Lipman, Chen, Ben-Hamu, Nickel & Le, Flow Matching for Generative Modeling (ICLR 2023)"
    href: "https://arxiv.org/abs/2210.02747"
  - label: "Karras, Aittala, Aila & Laine, Elucidating the Design Space of Diffusion-Based Generative Models (NeurIPS 2022)"
    href: "https://arxiv.org/abs/2206.00364"
  - label: "Esser et al., Scaling Rectified Flow Transformers for High-Resolution Image Synthesis (ICML 2024)"
    href: "https://arxiv.org/abs/2403.03206"
---
import Objectives from '../../../components/Objectives.astro';
import Prereq from '../../../components/Prereq.astro';
import Ask from '../../../components/Ask.astro';
import Remark from '../../../components/Remark.astro';
import Question from '../../../components/Question.astro';
import Answer from '../../../components/Answer.astro';
import QuizBlock from '../../../components/QuizBlock.astro';
import Option from '../../../components/Option.astro';
import Explanation from '../../../components/Explanation.astro';
import VizPlan from '../../../components/VizPlan.astro';
import Bridge from '../../../components/Bridge.astro';
import Ref from '../../../components/Ref.astro';

# 實作：同一個 toy，只換訓練目標

<Objectives>
- **在 <Ref week="dma-week-diffusion"/> 的同一個 toy 上實作線性 FM，並產出三張圖。** 訓練與 Euler／Heun 取樣、軌跡並排、步數對誤差的 log-log 圖、$\epsilon$ 模型轉速度場的驗證。
- **學會從 log-log 圖上分開讀「斜率」與「高度」。** 斜率是取樣器的階數、高度是路徑的彎曲程度，不要拿單一步數下的數字比較。
- **說出一個 diffusion 框架做不到、而這個 toy 只要改一行就能做的實驗：非 Gaussian 起點。** 並說出為什麼 diffusion 那一側做不到（作業 1 會真的把它跑出來）。
</Objectives>

<Prereq items={frontmatter.prereqs} />

## 設定：只動兩行

沿用 <Ref to="dma-lab-diffusion"/> 的 `toy.py`——同一組月牙資料、同一個 MLP、同一組 random seed。**唯一的改動是訓練目標與取樣器**，這樣圖上的差異才歸因得回框架，而不是「這次剛好訓得比較好」。

先確認符號沒有搞反：這個單元 `x0` 是**噪聲**、`x1` 是**資料**、`t` 從 0 走到 1，跟 <Ref week="dma-week-diffusion"/> 的 notebook 剛好對調。

<VizPlan id="w4-5-notebook" title="Flow Matching 單元實作 notebook" type="notebook">
- 沿用 `toy.py`，新增 `fm.py`：線性 FM 訓練、Euler／Heun 取樣器、`eps_to_velocity` 轉換函數。
- 四個 section 對應下面四個步驟。附上 <Ref week="dma-week-diffusion"/> 訓好的 $\epsilon_\theta$ 權重檔，讀者不必重跑上一個單元。
- 所有軌跡圖沿用 <Ref to="dma-lab-diffusion"/> 的座標範圍與配色。
</VizPlan>

## 步驟 1：訓練

```python
for step in range(n_steps):
    x1 = sample_data(batch)                    # 資料
    x0 = torch.randn_like(x1)                  # 噪聲
    t  = torch.rand(batch, 1)
    xt = t * x1 + (1 - t) * x0
    loss = ((model(xt, t) - (x1 - x0)) ** 2).mean()
    loss.backward(); opt.step(); opt.zero_grad()
```

和 <Ref to="dma-lab-diffusion"/> 的訓練迴圈逐行對照，真的只有 `xt` 與目標那兩行不同。

> **換框架在程式裡只是換兩行；換掉的是那兩行背後的設計自由度。**

**會看到的事情是**：loss 一樣不會掉到 0，而且停住的數字比上一個單元大。這不是壞事——<Ref to="dma-conditional-flow-matching"/> 說過 $\mathcal L_{\text{CFM}}$ 比 $\mathcal L_{\text{FM}}$ 多一個常數，就是條件速度繞著邊際速度的變異數。真正要看的是它**停在哪裡不動之後還會不會慢慢往下**。

## 步驟 2：取樣與軌跡圖（圖 a）

```python
def euler_sample(model, x0, n_steps):
    x, h, traj = x0, 1.0 / n_steps, [x0]
    for i in range(n_steps):
        t = torch.full((x.shape[0], 1), i * h)
        x = x + h * model(x, t)
        traj.append(x)
    return torch.stack(traj)
```

固定和 <Ref to="dma-lab-diffusion"/> 相同的 32 個起點（注意 FM 的起點是 $t=0$ 的噪聲，對應 DDPM 的 $x_T$），步數取 $\lbrace 10,50,1000 \rbrace$，把上一個單元的 DDIM 軌跡和這裡的 FM 軌跡並排。

**會看到的事情是**：兩邊的軌跡形狀明顯不同，而且在兩個月牙中間那塊區域都特別彎——那裡正是條件直線交叉最密的地方（<Ref to="dma-linear-path"/> Q1）。

<Ask>
那到底哪一條路徑的軌跡比較好走？<br />
這件事能不能量出來，而不是看圖猜？
</Ask>

## 步驟 3：步數對誤差（圖 b）

對步數 $\lbrace 2,5,10,20,50,100,1000 \rbrace$ 各取 2000 個樣本，算終點與資料的 $W_2$（用 `POT` 的精確 OT），DDIM 與 FM 各一條線，畫在 log-log 座標上。

這張圖有兩個獨立的訊息：

- **斜率**＝數值方法的階數。Euler 是一階，斜率應該接近 $-1$；換成 Heun（二階）會變成接近 $-2$。
- **高度**＝軌跡有多彎。同樣是 Euler，兩條線平行但高低不同，高的那條就是彎的那條。

先寫下你的猜測（哪條高？），再跑出來對。

<iframe loading="lazy" src="/personal_website/notes/research_areas/diffusion-models-applications/w4-5-steps.html" class="demo-frame" title="步數對誤差：斜率與高度：互動 demo" style="height: 830px; width: 100%; border: none;"></iframe>

**互動 demo：步數對誤差的 log-log 圖。** 這是圖 b 的「標準答案版」——速度場用資料精確算，所以看到的完全是離散化誤差，沒有網路的逼近誤差混進來。切「比方法」量到 Euler $\approx-1.05$、Heun $\approx-2.05$；切「比路徑」兩條線斜率一樣，但**線性路徑高出 VP 兩到三倍**。demo 只積到 $t=0.9$：最後那一小段 $\sigma_t\to0$ 太陡，會把階數的訊號蓋掉。

<Remark label="補充" summary="為什麼要用中位數，而不是平均">
少數幾條軌跡會在兩個 mode 之間的分岔點上「選錯邊」——步數少的時候走去左邊的月牙，步數多的時候走去右邊。這種樣本的誤差是 $O(1)$，跟步數幾乎無關，平均起來會把整條曲線壓平，斜率就讀不出來了。

> **量收斂階數之前，先確認你量的是離散化誤差，不是分岔選錯邊。**

取中位數就避開這件事。**這不是把不好看的資料藏起來**：分岔造成的誤差確實存在，但它和「離散化誤差隨步數怎麼縮」是兩個不同的現象，要分開量。跑自己的實驗時，值得把兩者都畫出來。
</Remark>

## 步驟 4：不重訓的轉換（圖 c）

載入 <Ref week="dma-week-diffusion"/> 的 $\epsilon_\theta$。它是在 VP 路徑上訓的，所以轉換時要用 VP 的 $(\alpha_t,\sigma_t)=(\sqrt{\bar\alpha_s},\sqrt{1-\bar\alpha_s})$，其中 $s$ 是把 FM 的 $t\in[0,1]$ 映回 DDPM 步數的那個對應。**注意它是反向的**：FM 的 $t=1$ 是資料那一端，對到的是 DDPM 的 $s=0$（$\bar\alpha_0=1$）；FM 的 $t=0$ 是噪聲端，對到 $s=T$。寫 `to_ddpm_step` 的時候先把這個邊界條件對一次，方向弄反了整張圖都會是錯的。

```python
def eps_to_velocity(eps_model, x, t):
    a, s, da, ds = vp_alpha(t), vp_sigma(t), vp_alpha_dot(t), vp_sigma_dot(t)
    eps = eps_model(x, to_ddpm_step(t))
    return (da / a) * (x - s * eps) + ds * eps
```

拿 `euler_sample` 去解這個速度場，和 DDIM 50 步的終點比對。**會看到的事情是**：兩邊幾乎重合，剩下的差來自兩個取樣器的時間格點對不齊，不是模型不同。這張圖就是 <Ref to="dma-fm-vs-diffusion"/>「同一個物件、兩套座標」的**數值**實證；那一篇的那個動手推導已經把**代數**那一半做完了——代 VP 進轉換式，兩個係數都化成 $-\beta/2$，正好是 PF-ODE。

<Question id="Q1">
把步驟 4 跑出來之後，很自然會想：既然 $\epsilon_\theta$ 轉一下就能當速度場，那 **<Ref week="dma-week-diffusion"/> 那個模型是不是也可以直接拿去用線性路徑的 FM 取樣器取樣？**

<Answer>
不行，而且這個坑很容易踩。

$\epsilon_\theta$ 學到的是 $\mathbb E[\epsilon\mid x_t]$，其中 $x_t=\sqrt{\bar\alpha_s}\,x_1+\sqrt{1-\bar\alpha_s}\,\epsilon$——它只在**VP 路徑的那一族中間分布上**見過資料。線性路徑的 $x_t=t\,x_1+(1-t)\,x_0$ 給出的是**不同的** $p_t$：同樣的 $t$，兩邊的訊噪比不一樣，點的分布也不一樣。

具體一點：VP 在 $\alpha^2+\sigma^2=1$ 上走，所以 $\mathbb E\|x_t\|^2$ 大致固定；線性路徑的 $\mathbb E\|x_t\|^2=t^2\mathbb E\|x_1\|^2+(1-t)^2 d$，在中間會**明顯縮小**。把線性路徑的 $x_t$ 餵進 VP 訓的網路，等於問它一個沒見過的分布上的問題，答案沒有理由是對的。

所以步驟 4 的轉換式裡，$(\alpha_t,\sigma_t)$ 必須是**模型當初訓練時用的那一條路徑**：「不用重訓」只換得了取樣器，換不了路徑。路徑是訓練時就寫進模型裡的東西。

這也順便解釋了 <Ref to="dma-fm-vs-diffusion"/> 為什麼要把四個旋鈕分開講：能事後換的（取樣器、座標）和不能事後換的（起點、路徑、配對、加權）不是同一類事情。
</Answer>
</Question>

<VizPlan id="w4-5-two-readings" title="log-log 圖的兩種讀法" type="static">
- 檔名 `imgs/w4-5-1.png`，放在 Q1 之後、「作業」之前。
- 版面：左右兩張小的 log-log 示意圖，橫軸都標「步數（對數）」、縱軸都標「誤差（對數）」。
  - **左「斜率＝方法的階數」**：兩條線從同一點出發、斜率不同，分別標 `Euler：斜率 −1` 與 `Heun：斜率 −2`；旁註「步數加倍：一階減半、二階掉四分之一」。
  - **右「高度＝軌跡有多彎」**：兩條**平行**的線（斜率都 −1），上面那條標 `軌跡比較彎`、下面那條標 `軌跡比較直`；在同一個步數上畫一條垂直的雙箭頭標出高度差。
- 底部一句：「比較兩個方法要看斜率，比較兩條路徑要看高度；只報一個步數下的數字，什麼都沒說。」
- 風格：與 `w-3-0-2.png` 一致（淺底、扁平、繁體中文標註、強調靛 `#2a4ea3`／路徑綠 `#60ba82`）；不要畫資料點雲，這張圖只有兩個座標軸和四條線。
- 長寬比：約 16:7。
</VizPlan>

## 作業

1. **非 Gaussian 起點。** 把 `x0` 換成 $[-2,2]^2$ 上的均勻分布，重訓 FM，畫軌跡。說明 <Ref week="dma-week-diffusion"/> 的框架為什麼做不到這件事——對應 <Ref to="dma-why-not-diffusion"/> Q1 的哪一個旋鈕？
2. **加權對照。** 訓兩個線性 FM 模型：一個均勻抽 $t$，一個從 logit-normal$(0,1)$ 抽 $t$。比較圖 b 的兩條曲線：斜率變了還是高度變了？先猜再跑，<Ref to="dma-shared-techniques"/> 會給答案。
3. **路徑對照（把一個軸單獨隔離出來）。** 保持獨立配對、保持 FM 目標，只把 $(\alpha_t,\sigma_t)$ 換成 VP 的 $(\sin\frac{\pi t}{2},\cos\frac{\pi t}{2})$ 重訓。它的軌跡比線性 FM 直還是彎？和上面 demo 量到的方向一致嗎？如果不一致，先懷疑「網路有沒有訓好」而不是「理論錯了」。
4. **分岔樣本。** 把步驟 3 的誤差改成平均而不是中位數，看曲線怎麼變形；再把「終點跑到另一個月牙」的樣本單獨挑出來畫。你會得到兩條性質完全不同的曲線。
5. **（選）Likelihood。** 用 Hutchinson 估計散度、沿 ODE 積分算測試點的 $\log p_1$，和月牙混合 Gaussian 的精確 log-density 比對。

## 消化一下

<QuizBlock id="w4-5-a">
圖 b 裡兩條 log-log 線斜率相同、高度不同。這說明什麼？
<Option>兩者用的是不同階的數值方法。</Option>
<Option correct>兩者都是一階 Euler，差異來自軌跡的彎曲程度。</Option>
<Option>其中一個網路訓得比較好。</Option>
<Explanation>
斜率由方法的階數決定，高度反映 $\int\|\ddot x_t\|\,dt$ 的大小——<Ref to="dma-error-theory"/> 會把這個量正式寫下來。
</Explanation>
</QuizBlock>

<QuizBlock id="w4-5-b">
步驟 4 中若省掉 `to_ddpm_step` 的時間對應、直接把 FM 的 $t$ 餵給 $\epsilon_\theta$，會怎樣？
<Option>完全正確，因為速度場與時間參數化無關。</Option>
<Option correct>會錯，因為 $\epsilon_\theta$ 對 $t$ 的語意是 DDPM 的步數，而且 $(\alpha_t,\sigma_t)$ 的導數也依賴這個對應。</Option>
<Option>只在 $t=0.5$ 附近會錯。</Option>
<Explanation>
「同一個 ODE」是**把時間對應好之後**才成立的。時間重參數化會改變速度的大小，但不改變軌跡的形狀。
</Explanation>
</QuizBlock>

<QuizBlock id="w4-5-c">
線性 FM 的訓練 loss 停在一個大於零的數字上，最合理的解讀是：
<Option>學習率太大，需要再調。</Option>
<Option>資料裡有標註噪聲。</Option>
<Option correct>$\mathcal L_{\text{CFM}}$ 比 $\mathcal L_{\text{FM}}$ 多一個不含 $\theta$ 的常數，那個常數就是條件速度繞著邊際速度的變異數。</Option>
<Explanation>
和 <Ref to="dma-lab-diffusion"/> 看到的 loss 停住是同一件事，只是主角從 $\epsilon$ 換成速度。要判斷有沒有訓好，得看依 $t$ 分箱之後的 loss，而不是那個總數。
</Explanation>
</QuizBlock>

<Bridge to="dma-unified-equation">
這個單元把 FM 建起來、證明它在 Gaussian 路徑上與 diffusion 是同一個 ODE、也把差異分到四個軸上。剩下的問題全部指向同一個地方：軌跡為什麼彎、彎多少、怎麼拉直、噪聲該不該加回去。下一個單元從一個把兩個框架都裝得進去的式子開始。
</Bridge>

## 參考文獻

1. Lipman, Y., Chen, R. T. Q., Ben-Hamu, H., Nickel, M., Le, M. [*Flow Matching for Generative Modeling.*](https://arxiv.org/abs/2210.02747) ICLR 2023. （步驟 1 的訓練目標。）
2. Karras, T., Aittala, M., Aila, T., Laine, S. [*Elucidating the Design Space of Diffusion-Based Generative Models.*](https://arxiv.org/abs/2206.00364) NeurIPS 2022. （把路徑、加權、取樣器當成可分開調的設計軸，並且是二階取樣器的標準參考。）
3. Esser, P., et al. [*Scaling Rectified Flow Transformers for High-Resolution Image Synthesis.*](https://arxiv.org/abs/2403.03206) ICML 2024. （作業 2 的 logit-normal 時間取樣出自這裡。）
