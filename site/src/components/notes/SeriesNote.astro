---
/**
 * SeriesNote.astro — 篇頭的「這是系列第幾篇、上一篇做了什麼、這一篇的範圍」。
 *
 * 這是 metadata，不是內容，所以**不能用 blockquote**——blockquote 在這個站是
 * 置中的匾額（見 claude/dma-notes-stop-and-emphasis.md），拿來放一段導言會
 * 變成一塊置中的長文字。這裡排成左對齊、灰、略小的 standfirst。
 *
 *   <SeriesNote>
 *   「Invariance and Equivariance」系列第 2 篇。上一篇…；這一篇…。
 *   </SeriesNote>
 */
---

<div class="series-note"><slot /></div>

<style>
  .series-note {
    margin: -0.2em 0 1.8em;
    padding-left: 14px;
    border-left: 2px solid var(--border);
    color: var(--text-muted);
    font-size: 0.93rem;
    line-height: 1.75;
  }
  .series-note :global(p) { margin: 0 0 0.5em; }
  .series-note :global(p:last-child) { margin-bottom: 0; }
  .series-note :global(strong) { color: var(--text-muted); font-weight: 600; }
  .series-note :global(a) { color: var(--text-muted); }
</style>
