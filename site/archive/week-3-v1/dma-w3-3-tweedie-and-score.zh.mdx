---
slug: "dma-w3-3-tweedie-and-score"
label: "dma-tweedie-and-score"
prereqs:
  - ml-bias-variance
  - math-conditional-expectation
  - math-mse-conditional-expectation
  - math-bayes-posterior
  - math-tweedie
  - math-total-derivative
lang: "zh"
title: "Tweedie 公式：Denoiser 就是 Score"
category: "courses"
group: "Week 3 · Diffusion Models"
status: "available"
updated: 2026-09-07
summary: "回歸學到的是條件期望；Tweedie 公式把 E[x₀|x_t] 與 ∇log p_t 綁成同一件事，回答「只用條件配對做回歸，為什麼會學到跟整個分布有關的量」。"
demos: []
references:
  - label: "Efron, Tweedie's Formula and Selection Bias (JASA 2011)"
    href: "https://efron.ckirby.su.domains/papers/2011TweediesFormula.pdf"
  - label: "Hyvärinen, Estimation of Non-Normalized Statistical Models by Score Matching (JMLR 2005)"
    href: "https://jmlr.org/papers/v6/hyvarinen05a.html"
  - label: "Song & Ermon, Generative Modeling by Estimating Gradients of the Data Distribution (NeurIPS 2019)"
    href: "https://arxiv.org/abs/1907.05600"
---
import Objectives from '../../../components/Objectives.astro';
import Prereq from '../../../components/Prereq.astro';
import Ask from '../../../components/Ask.astro';
import Remark from '../../../components/Remark.astro';
import Question from '../../../components/Question.astro';
import Answer from '../../../components/Answer.astro';
import QuizBlock from '../../../components/QuizBlock.astro';
import Option from '../../../components/Option.astro';
import Explanation from '../../../components/Explanation.astro';
import Bridge from '../../../components/Bridge.astro';
import Ref from '../../../components/Ref.astro';

# Tweedie 公式：Denoiser 就是 Score

<Objectives>
- **回答「只用條件配對做回歸，為什麼會學到邊際的量」。** 說出 MSE 的最佳解是條件期望、而條件期望本身就是一個對後驗的加權平均——這個模式之後會反覆出現。
- **推導 Tweedie 公式，把 $\mathbb E[x_0\mid x_t]$ 與 $\nabla\log p_t(x_t)$ 連起來。** 說出關鍵那一步（除以 $p_t(x)$ 之後被積函數按 Bayes 正好是後驗），以及這條等式靠的是什麼前提。
- **說出為什麼 score 這個建模對象完全不必理會 normalizing constant。** 並分清它是被**繞過**而不是被解決。
</Objectives>

<Prereq items={frontmatter.prereqs} />

## 回歸學到的，是條件期望

上一篇已經用過這個事實，這裡把它正式寫下來。對任何隨機變數 $Y$ 與輸入 $X$，

$$
\arg\min_f\ \mathbb E\big[\|f(X)-Y\|^2\big]=\mathbb E[Y\mid X].
$$

證明只要把 $Y$ 拆成 $\mathbb E[Y\mid X]+\big(Y-\mathbb E[Y\mid X]\big)$，交叉項的期望是零（<Ref to="math-mse-conditional-expectation"/>）。

套到上一篇的 loss，$X=x_t$、$Y=x_0$，於是：**容量夠、訓練到收斂的話，$\hat x_\theta(x_t,t)\to\mathbb E[x_0\mid x_t]$**；同理 $\epsilon_\theta\to\mathbb E[\epsilon\mid x_t]$。

這也是為什麼 denoising 回歸有意義：它學的不是「某一張圖」，而是「看到這個模糊的東西之後，所有可能原圖的平均」。

不過還有一個更根本的疑問沒解決。

<Ask>
我們從頭到尾只用了「一張圖 + 它的噪聲」這種配對，<br />
從來沒算過整批資料的分布。<br />
那模型憑什麼知道任何關於 $p_t$ 的事？
</Ask>

## 條件容易，邊際困難

<Question id="Q1">
訓練時我們每一筆資料都來自一個**已知的** $x_0$：加噪、得到 $(x_t,\epsilon)$、做回歸。我們**從來沒有算過**邊際分布

$$
p_t(x_t)=\int q(x_t\mid x_0)\,p_{\text{data}}(x_0)\,dx_0 .
$$

可是這一篇等一下要說：訓好的網路直接給出 $p_t$ 的 score $\nabla_{x_t}\log p_t(x_t)$——一個**只跟邊際有關**的量。

**它是怎麼從一堆條件配對裡冒出來的？**

<Answer>
機制就在上一節那一行：**MSE 的最佳解是條件期望，而條件期望本身就是一個對 $x_0$ 的加權平均。**

我們的確沒有算 $p_t$，但我們也不需要算——回歸在替我們做平均，而且權重剛好就是後驗 $p(x_0\mid x_t)$。上一篇的 demo 就是這件事的畫面：亮起來的那些點，就是被平均進去的 $x_0$，權重是它們「有多可能變成手上這個 $x_t$」。

我們只提供了容易的東西（一張圖、一個噪聲），平均這件事是 loss 自己做的。下一節的 Tweedie 公式，就是把「這個平均」與「score」寫成一條明確的等式。

這個模式值得記住，因為它會一直回來：

> **條件目標好算、邊際目標難算，但 MSE 回歸會自動把條件平均成邊際。**

<Ref week="dma-week-flow-matching"/> 的 conditional flow matching，整篇的精神就是這一句。
</Answer>
</Question>

## 把那個平均寫成一條等式：Tweedie 公式

固定一個 $t$，為了少寫字母，令 $a=\sqrt{\bar\alpha_t}$、$\sigma=\sigma_t$。邊際分布是一堆 Gaussian 的混合：

$$
p_t(x)=\int \mathcal N\!\big(x;\,a x_0,\ \sigma^2 I\big)\,p_{\text{data}}(x_0)\,dx_0 .
$$

對 $x$ 取梯度，而梯度可以穿到積分裡面去（只有 Gaussian 被微分）：

$$
\nabla_x p_t(x)
=\int \nabla_x\mathcal N\!\big(x;\,ax_0,\sigma^2I\big)\,p_{\text{data}}(x_0)\,dx_0
=\int -\frac{x-ax_0}{\sigma^2}\,\mathcal N\!\big(x;\,ax_0,\sigma^2I\big)\,p_{\text{data}}(x_0)\,dx_0 .
$$

接著兩邊除以 $p_t(x)$。這一步是整個推導的關鍵：右邊出現的 $\mathcal N(x;ax_0,\sigma^2I)\,p_{\text{data}}(x_0)\big/p_t(x)$，按 Bayes 定理**正好就是後驗** $p(x_0\mid x_t=x)$（<Ref to="math-bayes-posterior"/>）：

$$
\nabla_x\log p_t(x)
=\int -\frac{x-ax_0}{\sigma^2}\,p(x_0\mid x)\,dx_0
=-\frac{x-a\,\mathbb E[x_0\mid x_t=x]}{\sigma^2}.
$$

也就是說，只要把「對後驗取平均」這件事做完，score 就自己出現了。整理成兩個等價的寫法：

$$
\boxed{\;\mathbb E[x_0\mid x_t]=\frac{x_t+\sigma_t^2\,\nabla\log p_t(x_t)}{\sqrt{\bar\alpha_t}},\qquad
\nabla\log p_t(x_t)=-\frac{\mathbb E[\epsilon\mid x_t]}{\sigma_t}.\;}
$$

第二式直接把上一篇的 $\epsilon$-prediction 翻成 score：

$$
s_\theta(x_t,t):=-\frac{\epsilon_\theta(x_t,t)}{\sigma_t}\;\approx\;\nabla\log p_t(x_t).
$$

> **訓好的 denoiser 就是 score。**

我們用一支五行的回歸程式，拿到了一個原本以為得先處理 normalizing constant 才拿得到的東西。

不過整個推導靠著一個前提：**給定 $x_t$，$x_0$ 還有一個後驗可以平均**。這個前提後面會被打破——<Ref to="dma-unified-equation"/> 會看到，把配對換成確定性的映射之後，給定 $x_t$ 端點就定死了，沒有後驗可以平均，也就沒有可回歸的 score。

<Remark summary="為什麼 score 完全不必理會 normalizing constant？">
因為 score 是 $\log p$ 的**梯度**。如果 $p(x)=\tilde p(x)/Z$，那麼

$$
\nabla_x\log p(x)=\nabla_x\log\tilde p(x)-\underbrace{\nabla_x\log Z}_{=\,0},
$$

而 $Z$ 是常數、與 $x$ 無關，微分之後就消失了。<Ref to="dma-what-are-we-learning"/> 裡那個「$Z_\theta$ 的梯度把取樣困難拖進訓練」的問題，在這裡是被**繞過**而不是被解決：我們從頭到尾沒有去表示 $p_\theta$ 本身，只表示它的坡度。
</Remark>

## 這個 score 長什麼樣子？

回到 <Ref to="dma-what-are-we-learning"/> 那片地形。$\nabla\log p_t$ 指的是**站在目前位置、往密度上升最快的方向**——就是上坡最陡的那一邊。

- $t$ 很大時 $p_t\approx\mathcal N(0,I)$，地形是一座單峰的小丘，score $\approx-x$：不管站在哪裡，箭頭都指回原點。
- $t$ 很小時 $p_t\approx p_{\text{data}}$，地形回到原本崎嶇的樣子，箭頭指向最近的那一團資料。

而 denoiser 做的事，就是沿著這個坡走一步。下一篇的反向過程，就是把「沿著坡走」變成一個嚴格的取樣演算法。

![示意圖：左邊在等高線地形上，從 x_t 畫出兩支重疊的箭頭，分別標 ∇log p_t 與 √ᾱ_t·E[x₀|x_t] − x_t，並註明「方向相同，長度差 σ_t²」；右邊三張 x̂₀ 縮圖對應 t=100、500、900，從清楚的貓逐漸變成模糊的平均臉。](./imgs/w-3-3-1.png)

**圖：同一個方向，但 $t$ 太大時 $\hat x_0$ 會退化。** 左邊是 Tweedie 公式的畫面——兩支箭頭完全同向，只差 $\sigma_t^2$ 倍。右邊是它的另一面：$t$ 越大，$\hat x_0$ 從清楚的貓變成一張「平均臉」。**公式沒有變壞，是 $x_t$ 裡的資訊變少了。**

<iframe loading="lazy" src="/personal_website/notes/research_areas/diffusion-models-applications/w3-3-score.html" class="demo-frame" title="Score field 與 denoising 方向：互動 demo" style="height: 940px; width: 100%; border: none;"></iframe>

**互動 demo：score field 與 denoising 方向。** 箭頭是網格上每一點的 $\nabla\log p_t$（用資料樣本當混合 Gaussian 精確算出來的）。切到「denoising 方向」會看到 $\sqrt{\bar\alpha_t}\,\mathbb E[x_0\mid x_t]-x_t$ 的箭頭**方向完全一樣**，只差 $\sigma_t^2$ 倍——這就是 Tweedie 公式在畫面上的樣子。把 $t$ 拉大，箭頭會慢慢變成整片指向原點。

## 消化一下

<QuizBlock id="w3-3-a">
若 $\epsilon_\theta$ 訓到最佳，$\epsilon_\theta(x_t,t)$ 等於什麼？
<Option>訓練時抽到的那一個 $\epsilon$。</Option>
<Option correct>$\mathbb E[\epsilon\mid x_t]$：所有能產生這個 $x_t$ 的 $(x_0,\epsilon)$ 組合裡，$\epsilon$ 的平均。</Option>
<Option>$\nabla\log p_t(x_t)$。</Option>
<Explanation>
MSE 的最佳解是條件期望，所以是 $\mathbb E[\epsilon\mid x_t]$，不是訓練時抽到的那一個。而「等於 score」還差一個因子：$\nabla\log p_t=-\mathbb E[\epsilon\mid x_t]/\sigma_t$。
</Explanation>
</QuizBlock>

<QuizBlock id="w3-3-b">
Tweedie 公式的推導裡，「後驗 $p(x_0\mid x_t)$」是從哪一步冒出來的？
<Option>假設它是 Gaussian。</Option>
<Option correct>把 $\nabla p_t$ 兩邊除以 $p_t$ 時，被積函數 $\mathcal N(x;ax_0,\sigma^2)p_{\text{data}}(x_0)/p_t(x)$ 依 Bayes 定理正好是後驗。</Option>
<Option>對 $x_0$ 取梯度。</Option>
<Explanation>
整個推導只用了「梯度穿過積分」與 Bayes 定理，對 $p_{\text{data}}$ 沒有任何假設——這正是它強的地方。
</Explanation>
</QuizBlock>

<QuizBlock id="w3-3-c">
<Ref to="dma-what-are-we-learning"/> 說直接學密度會卡在 normalizing constant。score-based 的方法為什麼不卡？
<Option>因為它假設 $Z=1$。</Option>
<Option>因為它用蒙地卡羅估計 $Z$。</Option>
<Option correct>因為 $\nabla\log p=\nabla\log\tilde p-\nabla\log Z$，而 $Z$ 不依賴 $x$，這一項的梯度是零。</Option>
<Explanation>
score 對 normalizing constant 完全不敏感，這是它作為建模對象最根本的優勢。
</Explanation>
</QuizBlock>

<QuizBlock id="w3-3-d">
$t$ 很大時，$\nabla\log p_t(x)$ 大約長什麼樣子？
<Option correct>$\approx-x$：因為 $p_t$ 已經接近 $\mathcal N(0,I)$，箭頭處處指回原點。</Option>
<Option>$\approx 0$：因為分布很平坦。</Option>
<Option>指向最近的資料點，與 $t$ 無關。</Option>
<Explanation>
$\nabla\log\mathcal N(x;0,I)=-x$。這也是為什麼取樣的前幾步不太需要資料的細節，真正的內容是在 $t$ 變小之後才長出來的。
</Explanation>
</QuizBlock>

<Bridge to="dma-reverse-process">
兩件事現在都有了：<Ref to="dma-denoising-regression"/> 的 closed form 一步只缺 $\mathbb E[x_0\mid x_t]$，而我們剛剛確認網路給的就是它（也就是 score）。下一篇把 $\hat x_0$ 代進去、先算清楚這樣代要付什麼代價，再寫出真正的取樣器——而且會發現有兩種走法。
</Bridge>

## 參考文獻

1. Efron, B. [*Tweedie's Formula and Selection Bias.*](https://efron.ckirby.su.domains/papers/2011TweediesFormula.pdf) JASA 2011. （本篇主角公式的現代整理；歷史出處見 <Ref to="math-tweedie"/>。）
2. Hyvärinen, A. [*Estimation of Non-Normalized Statistical Models by Score Matching.*](https://jmlr.org/papers/v6/hyvarinen05a.html) JMLR 2005. （「不必知道 $Z$ 也能學」的起點。）
3. Song, Y., Ermon, S. [*Generative Modeling by Estimating Gradients of the Data Distribution.*](https://arxiv.org/abs/1907.05600) NeurIPS 2019. （把 score 當成生成模型的建模對象。）
